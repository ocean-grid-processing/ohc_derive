"""ohc_derive — per-product downstream quantities from an ohc_ingest store.

Single-product (no comparison): a registry of transforms f(product) -> derived variables,
collected into one mixed-dimension NetCDF. See README.md and derive_schema.md.
"""
